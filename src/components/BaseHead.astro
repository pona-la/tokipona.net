---
import { Font } from "astro:assets";

import ScriptToggleDarkMode from "@/components/ScriptToggleDarkMode.astro";

interface Props {
	title: string;
	description: string;
}
const siteUrl = new URL(import.meta.env.BASE_URL, Astro.site);
const socialImageURL = new URL("/logo.png", siteUrl);
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const siteTitle = siteUrl.hostname;
const url_goatcounter = "https://tokipona-net.goatcounter.com";
const noURL = Astro.routePattern == '/404';

const { title, description } = Astro.props;
---

<!-- Global Metadata -->
<meta charset='utf-8' />
<meta name='viewport' content='width=device-width,initial-scale=1' />
<link rel='icon' type='image/svg+xml' href='/favicon.svg' />
<link rel='icon' type='image/png' href='/logo.png' />
<link rel='sitemap' href='/sitemap-index.xml' />
<meta name='generator' content={Astro.generator} />

<!-- Font preloads -->
<Font cssVariable="--font-andika" preload />

{!noURL ?
	<!-- Canonical URL -->
	<link rel='canonical' href={canonicalURL} /> : ''}

<!-- Primary Meta Tags -->
<title>{title}</title>
<meta name='title' content={title} />
<meta name='description' content={description} />

<!-- Open Graph / Facebook -->
<meta property='og:type' content='website' />
{!noURL ? <meta property='og:url' content={Astro.url} /> : ''}
<meta property='og:site_name' content={siteTitle} />
<meta property='og:title' content={title} />
<meta property='og:description' content={description} />
<meta property='og:image' content={socialImageURL} />

<!-- Twitter -->
<meta property='twitter:card' content='summary' />
{!noURL ? <meta property='twitter:url' content={Astro.url} /> : ''}
<meta property='twitter:title' content={title} />
<meta property='twitter:description' content={description} />
<meta property='twitter:image' content={socialImageURL} />

<!-- Analytics -->
<script
	is:inline
	data-goatcounter={`${url_goatcounter}/count`}
	async
	src='//gc.zgo.at/count.js'></script>

<!-- Toggle scripts -->
<ScriptToggleDarkMode />
<!-- Astro components CSS -->

<slot />
