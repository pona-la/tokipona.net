---
const styles = `
* {
	font-family: "Andika", sans-serif;
}
.text-slate-500 {
	color: var(--foreground-muted);
}
section > *,
section > * > * {
	border:none !important;
}
.border-slate-300 {
	border: 3px solid color-mix(in srgb, var(--card-border), transparent 70%);
	border-radius: 10px;
}
:has(> article) {
	padding: 0;
}
section > :first-child {
	margin-top: 0;
}
section > * {
	margin-bottom: 0.5rem !important;
	&:not(:has(article)) {
		padding: 5px 15px !important;
	}
	& > article {
		padding: 5px 15px !important;
	}
}
a img {
	display: none
}
.flex.gap-2 {
	gap: 15px;
}
.flex.gap-2 > :first-child {
	width: 60px;
	height: 60px;
}
`;
---

<script
	type='module'
	src='https://cdn.jsdelivr.net/npm/bsky-embed/dist/bsky-embed.es.js'
	async></script>
<bsky-embed limit='2' username='tokipona.net' custom-styles={styles}>
</bsky-embed>
